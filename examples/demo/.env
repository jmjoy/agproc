# Loaded by the `backend` service through `env-file = ".env"`.
# The path is relative to the project root (this directory), and the values are
# injected into both build-cmd and run-cmd. The `env` table in agproc.toml wins
# over this file. A value with spaces must be quoted:
DEMO_GREETING="hello from env-file"
